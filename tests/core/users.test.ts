const mock = () => true;

describe("trueTest", () => {
  it("should return true", () => {
    expect(mock()).toBe(true);
  });
});

describe("getUsers", () => {
  it("should return an array of 'users'", () =>{
    
  })
})
